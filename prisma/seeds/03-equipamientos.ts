import { PrismaClient } from '@prisma/client';

/**
 * Seed 03: Equipamientos — DIFERIDO
 * El sistema de equipamientos está deshabilitado en la app web actual.
 * Ver MEMORY.md sección "Sistemas diferidos".
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export async function seedEquipamientos(_prisma: PrismaClient): Promise<void> {
  // sin datos — diferido
}
